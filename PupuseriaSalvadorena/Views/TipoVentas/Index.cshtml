@model IEnumerable<PupuseriaSalvadorena.Models.TipoVenta>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreVenta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreVenta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Estado)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTipoVenta">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTipoVenta">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTipoVenta">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
