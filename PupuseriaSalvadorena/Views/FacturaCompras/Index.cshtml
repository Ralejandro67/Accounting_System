@model IEnumerable<PupuseriaSalvadorena.Models.FacturaCompra>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FacturaCom)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaFactura)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TotalCompra)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DetallesCompra)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdTipoPago)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdTipoFactura)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FacturaCom)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaFactura)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TotalCompra)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DetallesCompra)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdTipoPago)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdTipoFactura)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdFacturaCompra">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdFacturaCompra">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdFacturaCompra">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
