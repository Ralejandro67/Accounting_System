@model IEnumerable<PupuseriaSalvadorena.Models.MateriaPrima>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreMateriaPrima)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdProveedor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreMateriaPrima)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdProveedor)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdMateriaPrima">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdMateriaPrima">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdMateriaPrima">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
