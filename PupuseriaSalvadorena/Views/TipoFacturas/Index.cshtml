@model IEnumerable<PupuseriaSalvadorena.Models.TipoFactura>

@{
    ViewData["Title"] = "Index";
}

@section Scripts {
    <script src="~/js/tipofactura.js" asp-append-version="true"></script>
}

<h1>Index</h1>

<div class="container mt-4 text-right">
    <button type="button" class="btn btn-primary mt-3" id="AddTipoF">
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-plus-lg" viewBox="0 0 16 16">
            <path fill-rule="evenodd" d="M8 2a.5.5 0 0 1 .5.5v5h5a.5.5 0 0 1 0 1h-5v5a.5.5 0 0 1-1 0v-5h-5a.5.5 0 0 1 0-1h5v-5A.5.5 0 0 1 8 2" />
        </svg>
    </button>
</div>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreFactura)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreFactura)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Estado)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTipoFactura">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTipoFactura">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTipoFactura">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

<div class="modal fade" id="newTipoFModal" tabindex="-1" role="dialog" aria-labelledby="newTipoFModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="newTipoFModalLabel">Nuevo Tipo de Factura</h5>
                <button type="button" class="close" data-bs-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                @Html.Partial("_newTipoFPartial", new PupuseriaSalvadorena.Models.TipoFactura())
            </div>
        </div>
    </div>
</div>