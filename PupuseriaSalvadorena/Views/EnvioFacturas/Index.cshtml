@model IEnumerable<PupuseriaSalvadorena.Models.EnvioFactura>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FechaEnvio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdFacturaVenta)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FechaEnvio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdFacturaVenta)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdEnvioFactura">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdEnvioFactura">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdEnvioFactura">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
