@model IEnumerable<PupuseriaSalvadorena.Models.CategoriaPresupuesto>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreCategoriaP)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreCategoriaP)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Estado)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdCategoriaP">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdCategoriaP">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdCategoriaP">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
